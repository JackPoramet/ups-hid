# ups_module -- Python dependencies (Linux)
# Install with: pip install -r requirements.txt

# ── Core HID Device Communication ─────────────────────────────────────────────
hidapi>=0.14.0      # hid module -- อ่าน Feature Report ผ่าน USB HID
